<<<<<<< HEAD:pengembalian_dan_interface/fitur pengembalian
void kembalikanBuku() {
    int pilihan;
    int ditemukan = 0;

    printf("\n=== Daftar Buku Dipinjam ===\n");

    // Tampilkan hanya buku yang dipinjam
    for (int i = 0; i < jumlahBuku; i++) {
        if (daftarBuku[i].dipinjam == 1) {
            printf("%d. %s\n", i + 1, daftarBuku[i].nama);
            ditemukan = 1;
        }
    }

    if (ditemukan == 0) {
        printf("Tidak ada buku yang sedang dipinjam.\n");
        return;
    }

    printf("\nPilih nomor buku yang ingin dikembalikan: ");
    scanf("%d", &pilihan);

    // Validasi input
    if (pilihan < 1 || pilihan > jumlahBuku) {
=======
void pengembalian(struct Peminjaman data[], int *jumlah) {
    int index;

    if (*jumlah == 0) {
        printf("Tidak ada alat yang dipinjam.\n");
        return;
    }

    // Tampilkan data
    printf("\n=== Daftar Peminjaman ===\n");
    for (int i = 0; i < *jumlah; i++) {
        printf("%d. %s - %s\n", i + 1, data[i].nama, data[i].namaAlat);
    }

    // Pilih data
    printf("\nMasukkan nomor yang dikembalikan: ");
    scanf("%d", &index);

    // Validasi
    if (index < 1 || index > *jumlah) {
>>>>>>> 2029bddf072af14c962aa69dcd70c3be7d7aca64:pengembalian dan interface/fitur pengembalian
        printf("Pilihan tidak valid!\n");
        return;
    }

<<<<<<< HEAD:pengembalian_dan_interface/fitur pengembalian
    // Proses pengembalian
    if (daftarBuku[pilihan - 1].dipinjam == 0) {
        printf("Buku sudah dikembalikan.\n");
    } else {
        daftarBuku[pilihan - 1].dipinjam = 0;
        printf("Buku '%s' berhasil dikembalikan!\n",
               daftarBuku[pilihan - 1].nama);
    }
=======
    // Hapus data (geser array)
    for (int i = index - 1; i < *jumlah - 1; i++) {
        data[i] = data[i + 1];
    }

    (*jumlah)--;

    printf("Pengembalian berhasil!\n");
>>>>>>> 2029bddf072af14c962aa69dcd70c3be7d7aca64:pengembalian dan interface/fitur pengembalian
}